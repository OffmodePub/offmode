// Re-exports from ThemeContext so all existing imports stay unchanged.
// Theme is now user-controllable via the Settings toggle (overrides system default).
export { useColors } from './ThemeContext';
